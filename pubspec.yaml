name: shader_playground
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.0-0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_shaders: ^0.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  shaders:
    - assets/shaders/chromatic_aberration.frag
    - assets/shaders/masked_blur.frag
  assets:
    - assets/images/
